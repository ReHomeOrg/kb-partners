"""Уведомления заявителю/партнёру/оператору (эпик E8, FR-8.1/8.2).

Паттерн — как у исходящих webhooks (`api/webhooks/`): эмиссия в transactional
outbox при переходах FSM (после commit), durable-дрейн воркером, веер по
config-gated seam-каналам (push/SMS/email). ПДн в payload не кладём — только
идентификаторы заявки + нейтральная RU-сводка статуса (152-ФЗ).
"""
