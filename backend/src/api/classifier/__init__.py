"""Классификатор категории заявки (эпик E2, §4.11).

Критичный модуль (CLAUDE-REVIEWER.md): на вход — ТОЛЬКО `raw_input_masked` (ПДн
замаскированы при приёме, FR-1.6); никаких ПДн в LLM и логах. Решение трассируемо
(метод/модель/версия/уверенность, FR-2.6). Быстрый детерминированный путь (правила)
минует LLM при однозначности (FR-2.2); неоднозначность/низкая уверенность →
сигнал к `NEEDS_REVIEW` (решает сервисный слой по порогу, FR-2.4).
"""

from __future__ import annotations
