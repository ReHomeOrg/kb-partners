"""Каналы доставки заявок партнёрам (эпик E4/E5, §9).

Критичный модуль (CLAUDE-REVIEWER.md): идемпотентность, подпись/replay-защита
входящих, отсутствие ПДн сверх состава, разрешённого по категории (FR-4.6).

Единый интерфейс `DeliveryChannel` (по образцу `LLMProvider`) + адаптеры
(Mock/PartnerApi/…). Все реализации обёрнуты в resilient-клиент (`api/clients`),
секреты — ссылкой на kb-vault (не инлайн). Новый канал = новая реализация
интерфейса + запись в `PartnerChannelConfig`, без изменения ядра.
"""

from __future__ import annotations
