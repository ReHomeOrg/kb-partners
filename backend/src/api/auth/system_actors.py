"""Реестр системных акторов аудита kb-partners.

`RequestHistory.actor_id` — NOT NULL (инвариант: «у каждой записи аудита есть
актор»). Изменения, инициированные не человеком, а системным процессом (движок
автоматизации, SLA-таймер, диспетчер каналов, ИИ-агент), фиксируются в
неизменяемом журнале под **резервным sentinel-UUID** из диапазона
`00000000-0000-0000-0000-0000000000XX`, который Keycloak как random-v4 не
выдаёт → коллизия с реальным user-id исключена.

Для трассируемости конкретики (какое правило/канал) вызывающий кладёт доменный id
(напр. `automation_rule_id`, `channel_type`) в `to_value` записи истории.
"""

from __future__ import annotations

import uuid

# Движок автоматизации (E6): актор изменений по AutomationRule.
AUTOMATION_ACTOR_ID = uuid.UUID("00000000-0000-0000-0000-0000000000a0")

# SLA-таймер (E6): актор breach/эскалаций по дедлайнам.
SLA_ACTOR_ID = uuid.UUID("00000000-0000-0000-0000-0000000000a1")

# Воркер ретенции ПДн (NFR-12, 152-ФЗ): актор обезличивания просроченного raw_input.
RETENTION_ACTOR_ID = uuid.UUID("00000000-0000-0000-0000-0000000000a2")

# Диспетчер каналов (E4/E5): актор авто-fallback и продвижения статуса из inbound.
DISPATCH_ACTOR_ID = uuid.UUID("00000000-0000-0000-0000-0000000000d0")

# ИИ-агент-оркестратор «Консьерж» (E9): сервис-принципал инфраструктурных действий
# агента. ВАЖНО: действия агента ОТ ИМЕНИ пользователя атрибутируются пользователю
# (on-behalf-of, FR-9.7), а не этому sentinel — он только для инфраструктурных вызовов.
AGENT_ACTOR_ID = uuid.UUID("00000000-0000-0000-0000-0000000000c1")
