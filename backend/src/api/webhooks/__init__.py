"""Исходящие доменные webhooks (E8, §11.4).

События эмитятся ПОСЛЕ commit через transactional outbox (kind `webhook`): продюсер
ставит сообщение в той же транзакции, что и доменное изменение; воркер доставляет
подписчику с HMAC-подписью. В payload — без ПДн (событие/номер/статус/ссылки).
"""

from __future__ import annotations
